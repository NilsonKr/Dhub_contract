export type UserInfo = {
  name: string;
  profileUrl: string;
};

export type FileInfo = {
  id: number;
  url: string;
  title: string;
  description: string;
  uploadDate: string;
  size: number;
};
